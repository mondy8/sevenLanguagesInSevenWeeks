%セルフスタディ2日目-1
reverse([], []). % 空リストの場合、逆順も空リスト。再帰処理の必ず終了条件
reverse([H|T], Y) :- % リスト[H|T]の逆順をYにする
    reverse(T, RevT), % Tを逆順にしたリストをまず求める
    append(RevT, [H], Y). % そのリストにHを追加してYを作る

% 例えば、reverse([1, 2, 3], Y)の場合、再帰が進むごとに以下のように処理される：
% 1.reverse([2, 3], RevT)が呼ばれ、さらにその中でreverse([3], RevT)が呼ばれる。
% 2.最終的にreverse([], [])に到達して再帰が終わり、逆順に構築が始まる。
% 3,append([], [3], [3])、次にappend([3], [2], [3, 2])、最後にappend([3, 2], [1], [3, 2, 1])となる。
