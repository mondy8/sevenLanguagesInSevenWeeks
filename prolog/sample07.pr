%セルフスタディ2日目-2
maxlist([X],X).　% リストが1つの要素だけの場合、その要素が最大
maxlist([H|T],X) :- maxlist(T,Y), X is max(H,Y).

% 例えば、maxlist([3, 5, 2], X)の場合：
% maxlist([5, 2], Y)が呼ばれ、さらにmaxlist([2], Z)が呼ばれる。
% maxlist([2], Z)はベースケースなので、Z = 2を返す。
% 戻ってきて、max(5, 2)が計算され、Y = 5になる。
% さらに戻ってきて、max(3, 5)が計算され、最終的にX = 5になる。
